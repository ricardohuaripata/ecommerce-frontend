import { SizeListSortPipe } from './size-list-sort.pipe';

describe('SizeListSortPipe', () => {
  it('create an instance', () => {
    const pipe = new SizeListSortPipe();
    expect(pipe).toBeTruthy();
  });
});
